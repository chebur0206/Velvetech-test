import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles({
  createButtonWrapper: {
    textAlign: "center",
    margin: "2vh 16vw 2vh 0",
  },
});

export default useStyles;
